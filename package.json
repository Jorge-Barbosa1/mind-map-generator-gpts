{
  "name": "mind-map-generator-gpts",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd backend && python -m uvicorn app.main:app --reload --port 8000\" \"npm --prefix frontend start\" --names \"backend,frontend\" --prefix-colors \"blue,green\"",
    "install-all": "pip install -r backend/requirements.txt && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@react-pdf-viewer/core": "^3.12.0",
    "pdfjs-dist": "^3.4.120"
  }
}
